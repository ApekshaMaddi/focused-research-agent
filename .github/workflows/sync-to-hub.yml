name: Sync to Hugging Face Hub

on:
  push:
    branches: [master]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to Hugging Face Hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config user.email "apekshamaddi22@gmail.com"
          git config user.name "ApekshaMaddi"
          git remote add hf https://ApekshaMaddi:${HF_TOKEN}@huggingface.co/spaces/ApekshaMaddi/focused-research-agent
          git push hf master:main --force